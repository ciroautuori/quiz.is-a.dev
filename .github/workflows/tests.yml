name: Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test:
    name: Lint & Type Check
    runs-on: [self-hosted, soliso, devquest]

    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v4 pin SHA

      - name: Type Check & Build
        run: |
          npm install --no-audit --no-fund
          npx tsc --noEmit
          npm run build
